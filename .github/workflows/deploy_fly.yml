name: Code Check

on:
  push:
    tags:
      - '*'

jobs:
  fly:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install fly.io
        run : |
          echo "Deploy to fly.io..."